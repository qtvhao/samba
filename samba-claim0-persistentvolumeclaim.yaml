apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: samba-claim0
  name: samba-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
